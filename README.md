# ArduinoProject
This project is two parts, an LCD connected to a RFID reader (mfrc522) and a simple keypad code that when the correct password is entered, the LED turns green, otherwise turns red. Both are seperate codes!!

To find out the UID of a key card/fob, use a RFID reader code.
